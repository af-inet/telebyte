# RATE = 41000 / 2
RATE = 44100 * 2

# RATE = 44100
# RATE = 20500
# SYMBOL_SIZE = int(RATE * 0.1)
SYMBOL_SIZE = int(RATE / 100.0)
FREQ_0 = SYMBOL_SIZE / 2
FREQ_1 = SYMBOL_SIZE / 4
SYNC_WORD = "SS"
SYNC_WORD_BITS = len(SYNC_WORD) * 8
SYNC_WORD_SIZE = SYNC_WORD_BITS * SYMBOL_SIZE
BUFFER_SIZE = 1024 * 8
FRAME_DATA = "The Quick Brown Fox"
FRAME_SIZE = len(FRAME_DATA) * 8